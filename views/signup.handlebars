<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Signup | iService</title>
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
</head>

<body>
    <div class="min-h-screen flex items-center justify-center bg-gray-50 pb-12 pb-6 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full space-y-8">
            <div>
                <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
                    Create a new account
                </h2>
                <p class="mt-2 text-center text-sm text-gray-600">
                    Or
                    <a href="/login" class="font-medium text-indigo-600 hover:text-indigo-500">
                        sign-in
                    </a>
                </p>
            </div>
            <form class="mt-8 space-y-6" action="/signup" method="POST">
                <div class="rounded-md shadow-sm">
                    <div class="flex mb-2 space-x-2">
                        <div class="flex-grow">
                            <label for="first-name" class="block text-sm font-medium text-gray-700 mb-1">First
                                Name</label>
                            <input id="first-name" name="first_name" type="text" autocomplete="first-name" required
                                class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.first_name}} border-red-700 {{/if}}"
                                placeholder="First Name" value="{{old_data.first_name}}">
                            {{#if errors.first_name}}
                            <span class="text-xs text-red-700">{{errors.first_name.msg}}</span>
                            {{/if}}
                        </div>
                        <div class="flex-grow">
                            <label for="last-name" class="block text-sm font-medium text-gray-700 mb-1">Last
                                Name</label>
                            <input id="last-name" name="last_name" type="text" autocomplete="last-name" required
                                class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.last_name}} border-red-700 {{/if}}"
                                placeholder="Last Name" value="{{old_data.last_name}}">
                            {{#if errors.last_name}}
                            <span class="text-xs text-red-700">{{errors.last_name.msg}}</span>
                            {{/if}}
                        </div>
                    </div>
                    <div class="mb-2">
                        <label for="email-address" class="block text-sm font-medium text-gray-700 mb-1">Email
                            address</label>
                        <input id="email-address" name="email" type="email" autocomplete="email" required
                            class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.email}} border-red-700 {{/if}}"
                            placeholder="name@email.com" value="{{old_data.email}}">
                        {{#if errors.email}}
                        <span class="text-xs text-red-700">{{errors.email.msg}}</span>
                        {{/if}}
                    </div>
                    <div class="mb-2">
                        <label for="password" class="block text-sm font-medium text-gray-700 mb-1">Password</label>
                        <input id="password" name="password" type="password" autocomplete="new-password" required
                            class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.password}} border-red-700 {{/if}}"
                            placeholder="••••••••">
                        {{#if errors.password}}
                        <span class="text-xs text-red-700">{{errors.password.msg}}</span>
                        {{/if}}
                    </div>
                    <div class="mb-2">
                        <label for="password-confirm"
                            class="block text-sm font-medium text-gray-700 mb-1">Confirm Password</label>
                        <input id="password-confirm" name="password_confirm" type="password"
                            autocomplete="confirm-password" required
                            class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 rounded focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.password}} border-red-700 {{/if}}"
                            placeholder="••••••••">
                        {{#if errors.password_confirm}}
                        <span class="text-xs text-red-700">{{errors.password_confirm.msg}}</span>
                        {{/if}}
                    </div>
                    <div class="mb-2">
                        <label for="phone-number" class="block text-sm font-medium text-gray-700 mb-1">Phone
                            Number</label>
                        <input id="phone-number" name="phone_number" type="text" autocomplete="off" required
                            class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.phone_number}} border-red-700 {{/if}}"
                            placeholder="04xx xxx xxx" value="{{old_data.phone_number}}">
                        {{#if errors.phone_number}}
                        <span class="text-xs text-red-700">{{errors.phone_number.msg}}</span>
                        {{/if}}
                    </div>
                    <div class="mb-2">
                        <label for="street-address-line1"
                            class="block text-sm font-medium text-gray-700 mb-1">Address</label>
                        <input id="street-address-line1" name="street_line_1" type="text" autocomplete="off" required
                            class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.street_line_1}} border-red-700 {{/if}}"
                            placeholder="Parliament Dr" value="{{old_data.street_line_1}}">
                        {{#if errors.street_line_1}}
                        <span class="text-xs text-red-700">{{errors.street_line_1.msg}}</span>
                        {{/if}}
                        <input id="street-address-line2" name="street_line_2" type="text" autocomplete="off"
                            class="mt-2 appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.street_line_2}} border-red-700 {{/if}}"
                            placeholder="" value="{{old_data.street_line_2}}">
                    </div>
                    <div class="mb-2 flex mb-2 space-x-2">
                        <div class="flex-grow">
                            <label for="city" class="block text-sm font-medium text-gray-700 mb-1">City</label>
                            <input id="city" name="city" type="text" autocomplete="off" required
                                class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.city}} border-red-700 {{/if}}"
                                placeholder="Canberra" value="{{old_data.city}}">
                            {{#if errors.city}}
                            <span class="text-xs text-red-700">{{errors.city.msg}}</span>
                            {{/if}}
                        </div>
                        <div class="flex-grow">
                            <label for="state" class="block text-sm font-medium text-gray-700 mb-1">State</label>
                            <input id="state" name="state" type="text" autocomplete="off" required
                                class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.state}} border-red-700 {{/if}}"
                                placeholder="ACT" value="{{old_data.state}}">
                            {{#if errors.state}}
                            <span class="text-xs text-red-700">{{errors.state.msg}}</span>
                            {{/if}}
                        </div>
                        <div class="flex-grow">
                            <label for="postcode" class="block text-sm font-medium text-gray-700 mb-1">Postcode</label>
                            <input id="postcode" name="postcode" type="text" autocomplete="off"
                                class="appearance-none rounded relative block w-full px-3 py-2 border border-gray-300 placeholder-gray-500 text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.postcode}} border-red-700 {{/if}}"
                                placeholder="2600" value="{{old_data.postcode}}">
                            {{#if errors.postcode}}
                            <span class="text-xs text-red-700">{{errors.postcode.msg}}</span>
                            {{/if}}
                        </div>
                    </div>
                    <div class="mb-2">
                        <label for="country" class="block text-sm font-medium text-gray-700 mb-1">Country</label>
                        <div class="relative inline-block w-full">
                            <select id="country" name="country_of_residence"
                                class="w-full h-10 px-3 py-2 text-base placeholder-gray-500 border rounded appearance-none text-gray-900 focus:outline-none focus:ring-indigo-500 focus:border-indigo-500 focus:z-10 sm:text-sm {{#if errors.postcode}} border-red-700 {{/if}}"
                                placeholder="-- Select One --" value="{{old_data.postcode}}">
                                <option value="australia">Australia</option>
                                <option value="usa">USA</option>
                            </select>
                            <div class="absolute inset-y-0 right-0 flex items-center px-2 pointer-events-none">
                                <svg class="w-4 h-4 fill-current" viewBox="0 0 20 20">
                                    <path
                                        d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z"
                                        clip-rule="evenodd" fill-rule="evenodd"></path>
                                </svg>
                            </div>
                        </div>
                        {{#if errors.country}}
                        <span class="text-xs text-red-700">{{errors.country.msg}}</span>
                        {{/if}}
                    </div>
                </div>

                <div>
                    <button type="submit"
                        class="group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500">
                        <span class="absolute left-0 inset-y-0 flex items-center pl-3">
                            <svg class="h-5 w-5 text-indigo-500 group-hover:text-indigo-400"
                                xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor"
                                aria-hidden="true">
                                <path fill-rule="evenodd"
                                    d="M5 9V7a5 5 0 0110 0v2a2 2 0 012 2v5a2 2 0 01-2 2H5a2 2 0 01-2-2v-5a2 2 0 012-2zm8-2v2H7V7a3 3 0 016 0z"
                                    clip-rule="evenodd" />
                            </svg>
                        </span>
                        Sign Up
                    </button>
                </div>
            </form>
        </div>
    </div>
</body>

</html>